diff --git a/node_modules/@10play/tentap-editor/src/RichText/RichText.tsx b/node_modules/@10play/tentap-editor/src/RichText/RichText.tsx
index 88867f9..8326dd9 100644
--- a/node_modules/@10play/tentap-editor/src/RichText/RichText.tsx
+++ b/node_modules/@10play/tentap-editor/src/RichText/RichText.tsx
@@ -48,7 +48,6 @@ export const RichText = ({
   ...props
 }: RichTextProps) => {
   const [editorHeight, setEditorHeight] = useState(0);
-  const [key, setKey] = useState('webview');
   const [loaded, setLoaded] = useState(isFabric());
   const { keyboardHeight, isKeyboardUp } = useKeyboard();
   const source: WebViewProps['source'] = editor.DEV
@@ -116,6 +115,10 @@ export const RichText = ({
     () => getInjectedJS(editor.bridgeExtensions || []),
     [editor.bridgeExtensions]
   );
+  const injectedJavaScriptBeforeContentLoaded = useMemo(
+    () => getInjectedJSBeforeContentLoad(editor),
+    [editor]
+  );
 
   return (
     <>
@@ -124,7 +127,7 @@ export const RichText = ({
       )}
       <WebView
         scrollEnabled={false}
-        key={key}
+        key="webview"
         style={[
           RichTextStyles.fullScreen,
           { display: loaded ? 'flex' : 'none' },
@@ -136,9 +139,9 @@ export const RichText = ({
         ]}
         source={source}
         injectedJavaScript={injectedJavaScript}
-        injectedJavaScriptBeforeContentLoaded={getInjectedJSBeforeContentLoad(
-          editor
-        )}
+        injectedJavaScriptBeforeContentLoaded={
+          injectedJavaScriptBeforeContentLoaded
+        }
         hideKeyboardAccessoryView={true}
         onMessage={onWebviewMessage}
         ref={editor.webviewRef}
@@ -148,11 +151,13 @@ export const RichText = ({
         // Propagated Props
         onLoad={(e) => {
           setLoaded(true);
-          // This is a workaround for iOS to make sure the webview is loaded
-          // See https://github.com/react-native-webview/react-native-webview/issues/3578
-          if (Platform.OS === 'ios' && key === 'webview') {
-            setKey('webview_reloaded');
-          }
+          // Fabric can load the HTML before the before-content script runs.
+          // Re-inject the bridge configuration once after load as a guarded fallback.
+          setTimeout(() => {
+            editor.webviewRef.current?.injectJavaScript(
+              `if (!window.contentInjected) { ${injectedJavaScriptBeforeContentLoaded} } true;`
+            );
+          }, 50);
           props.onLoad && props.onLoad(e);
         }}
       />
diff --git a/node_modules/@10play/tentap-editor/src/webEditorUtils/focusListener.tsx b/node_modules/@10play/tentap-editor/src/webEditorUtils/focusListener.tsx
index 0c0f55f..86026d7 100644
--- a/node_modules/@10play/tentap-editor/src/webEditorUtils/focusListener.tsx
+++ b/node_modules/@10play/tentap-editor/src/webEditorUtils/focusListener.tsx
@@ -1,10 +1,12 @@
-import { isExpo } from '../utils/misc';
 class FocusListener {
   private focus: boolean;
   constructor() {
     this.focus = false;
+    const documentRef =
+      typeof window !== 'undefined' ? window.document : undefined;
+    if (!documentRef?.addEventListener) return;
     // @ts-ignore
-    window.document.addEventListener(
+    documentRef.addEventListener(
       'focus',
       () => {
         this.focus = true;
@@ -12,7 +14,7 @@ class FocusListener {
       true
     );
     // @ts-ignore
-    window.document.addEventListener(
+    documentRef.addEventListener(
       'blur',
       () => {
         this.focus = false;
@@ -28,6 +30,6 @@ class FocusListener {
 // For some reason on expo, this file is parsed on native, and then we get an error
 // when bundling because "document" does not exist. This is a hack to "shim" focusListener on expo
 const shimmedFocusListener = { isFocused: false };
-export const focusListener = isExpo()
-  ? shimmedFocusListener
-  : new FocusListener();
+// This source entry is evaluated by React Native. The actual web editor uses
+// TenTap's prebuilt web bundle, so native must always use the DOM-free shim.
+export const focusListener = shimmedFocusListener;
